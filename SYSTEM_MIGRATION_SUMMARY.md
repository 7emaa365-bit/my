# نظام إدارة الفروع المستقلة - وثائق التطبيق

## My Order - Multi-Branch Independent Management System

---

## 📋 ملخص التطبيق

تم تحويل تطبيق **My Order** من نظام مطعم موحد إلى **نظام إدارة فروع مستقل تماماً** حيث:

- كل فرع يعمل في **بيئة منعزلة تماماً** (كل فرع له بيانات منفصلة)
- **لا توجد تعارضات بين الفروع** - كل فرع يدير أسعاره وقائمته وطلباته بشكل مستقل
- **بوابة موحدة** (Gateway) هي نقطة الدخول الوحيدة لاختيار الفرع

---

## 🎯 المميزات الرئيسية المطبقة

### 1️⃣ **المفاتيح الديناميكية (Dynamic Keying)**

- **مفتاح التخزين:** `store_data_<slug>`
  - مثال: `store_data_مطعم-البيتزا-الإيطالي`
  - يحتوي على: **المنيو، الطلبات، الإعدادات بالكامل**

### 2️⃣ **استقلال البيانات الكامل**

كل فرع يحتفظ بـ:

```javascript
{
  slug: "branch-slug",
  name: "اسم الفرع",
  logo: "https://...",
  phone: "20101234567",        // رقم واتساب الفرع الحصري
  menu: [...items],             // قائمة منفصلة تماماً
  orders: [...orders],          // طلبات الفرع فقط
  description: "...",
  hours: "10:00 - 23:00",
  addresses: [...],
  social: {...}
}
```

### 3️⃣ **لوحة التحكم المستقلة**

عند دخول الإدارة من داخل فرع معين:

- ✅ تعديل الأسعار والأصناف يؤثر على الفرع فقط
- ✅ سجل المبيعات يعرض طلبات الفرع حصراً
- ✅ زر تغيير الحالة يؤثر على هذا الفرع فقط

### 4️⃣ **إعدادات الفرع (Store Settings)**

قسم متكامل داخل لوحة التحكم يسمح بـ:

- 📝 تعديل **اسم الفرع**
- 📱 تحديث **رقم واتساب الفرع** (يُستخدم في جميع الرسائل)
- 🏪 إضافة **وصف الفرع والعنوان**
- 🔗 إدارة **روابط التواصل** (انستجرام، فيسبوك، إلخ)
- ⏰ تعيين **ساعات العمل**

### 5️⃣ **نظام التحميل الذكي (Smart Loading)**

عند فتح صفحة فرع جديد:

- إذا **لم توجد بيانات محفوظة**، يتم نسخ **المنيو الافتراضي تلقائياً**
- ينطبق هذا على **جميع الفروع** (كل فرع يبدأ بنفس القائمة الأساسية)

### 6️⃣ **التوجيه من خلال البوابة (Gateway)**

- **gateway.html** هو المدخل الوحيد
- يعرض جميع الفروع المتاحة
- عند النقر على أي فرع: `index.html?res=<branch-name>`
- إذا حاول أحدهم فتح `index.html` مباشرة: يُعاد توجيهه للـ Gateway تلقائياً

### 7️⃣ **رسائل الواتساب الذكية**

- عند إتمام طلب: يتم إرسال الطلب لـ **رقم الفرع الحصري**
- العميل يتلقى رقم الفرع نفسه في التأكيد
- إعادة الإرسال تستخدم دائماً رقم الفرع الصحيح

---

## 📊 هيكل البيانات الجديد

### localStorage Keys:

```
store_data_مطعم-البيتزا-الإيطالي    ← فرع 1 (جميع البيانات)
store_data_برجر-بازوكا              ← فرع 2 (جميع البيانات)
store_data_مطعم-السوشي-الياباني    ← فرع 3 (جميع البيانات)
store_data_دجاج-ستريت              ← فرع 4 (جميع البيانات)
store_data_كافيه-الفن-الحديث       ← فرع 5 (جميع البيانات)

allOrders                          ← سجل مركزي (تاريخي، للتوافقية)
myReviews                          ← التقييمات العامة
customers                          ← العملاء المسجلين
```

---

## 🔧 التعديلات التقنية في script.js

### أ) دوال التوافق الأساسية:

```javascript
window.getBranchKey(); // ← "store_data_<slug>"
window.getMenuStorageKey(); // ← نفس getBranchKey() (branch object موحد)
window.CURRENT_RESTAURANT; // ← slug الفرع من URL
window.BRANCH_DATA; // ← كائن الفرع الكامل
```

### ب) دوال تحميل البيانات:

```javascript
loadBranchData(); // تحميل أو إنشاء بيانات الفرع
saveBranchData(branchObj); // حفظ البيانات في مفتاح branch الخاص
```

### ج) الدوال المُحدَّثة:

- ✅ `renderAllOrders()` - تعرض طلبات الفرع حصراً
- ✅ `loadCustomerOrders()` - تاريخ الطلبات الشخصي (فرع واحد)
- ✅ `printInvoice()` - تطبع من بيانات الفرع
- ✅ `finishOrder()` - تحفظ في الفرع + السجل المركزي
- ✅ `updateOrderStatus()` - تحدّث حالة الطلب في الفرع
- ✅ `updateAdminStatistics()` - الإحصائيات من الفرع فقط
- ✅ `initializeAdminPanel()` - لوحة تحكم الفرع مع الإعدادات

---

## 🚀 سير العمل المتوقع

### للعميل:

1. يفتح `gateway.html`
2. يختار الفرع المطلوب
3. يُنقل إلى `index.html?res=<branch-name>`
4. يرى قائمة الفرع وأسعاره
5. عند الطلب: يُرسل الطلب لرقم الفرع الخاص

### لمدير الفرع:

1. يدخل لوحة التحكم من داخل فرعه
2. يرى **إدارة منفصلة تماماً**
3. يعدّل الأسعار والقائمة (خاصة به)
4. يرى طلبات فرعه فقط
5. يحدّث رقم الواتساب والعنوان الخاص به
6. يطبع فاتورات طلبات فرعه

### للمدير العام (أمين المستودع):

- يمكنه رؤية جميع الفروع من خلال **البوابة الموحدة**
- حسابات منفصلة لكل فرع
- إحصائيات شاملة (في المستقبل)

---

## 📱 مثال عملي: طلب جديد

### الموقف:

عميل يطلب من **مطعم البيتزا الإيطالي**

### الخطوات:

1. **gateway.html** → اختيار المطعم
2. **index.html?res=مطعم-البيتزا-الإيطالي**
   - تحميل: `store_data_مطعم-البيتزا-الإيطالي`
   - عرض قائمة البيتزا بأسعار البيتزا
   - `RESTAURANT_PHONE` = رقم البيتزا (مثلاً: 201001234567)

3. **إتمام الطلب**

   ```javascript
   // يتم حفظ في:
   - store_data_مطعم-البيتزا-الإيطالي.orders ← طلب البيتزا
   - localStorage.allOrders ← نسخة احتياطية

   // يتم الإرسال إلى:
   wa.me/201001234567 ← رقم البيتزا الحصري
   ```

4. **لوحة التحكم للبيتزا**
   - تعرض فقط طلبات البيتزا
   - تعديل سعر البيتزا لا يؤثر على فروع أخرى
   - إحصائيات البيتزا منفصلة تماماً

---

## ✨ الفوائد الرئيسية

| الميزة           | الوصف                                        |
| ---------------- | -------------------------------------------- |
| **العزل الكامل** | كل فرع مستقل تماماً، لا تأثير متبادل         |
| **سهل الإدارة**  | مدير كل فرع يدير فرعه فقط                    |
| **قابل للتوسع**  | إضافة فروع جديدة سهلة جداً                   |
| **آمن**          | بيانات كل فرع محمية في مفتاح خاص             |
| **موثوق**        | نسخ احتياطية من الطلبات في السجل المركزي     |
| **ذكي**          | تحميل تلقائي للمنيو الافتراضي للفروع الجديدة |

---

## 🔒 الأمان والصحة

✅ **كل فرع محمي بـ:**

- مفتاح تخزين فريد
- بيانات منعزلة تماماً
- رقم واتساب خاص
- إعدادات خاصة

✅ **الفروع الموجودة:**

1. مطعم البيتزا الإيطالي
2. برجر بازوكا
3. مطعم السوشي الياباني
4. دجاج ستريت
5. كافيه الفن الحديث

---

## 📝 ملاحظات المطور

### للدعم المستقبلي:

- إذا أردت إضافة فرع جديد: أضفه في `restaurants[]` داخل script.js
- البوابة تُحدَّث تلقائياً
- المنيو يُنسخ تلقائياً عند الفتح الأول

### للنسخ الاحتياطية:

- احتفظ بنسخة من `allOrders` للتاريخ العام
- كل فرع محفوظ في `store_data_*` الخاص به

---

## 🎓 أمثلة الكود

### دخول فرع واختيار بيانات الفرع:

```javascript
const raw = getQueryParam("res"); // مثلاً: "مطعم-البيتزا-الإيطالي"
window.CURRENT_RESTAURANT = raw.replace(/\s+/g, "-");
const branchKey = `store_data_${window.CURRENT_RESTAURANT}`;
window.BRANCH_DATA = loadBranchData(); // تحميل بيانات الفرع الكاملة

// الآن:
// RESTAURANT_PHONE = window.BRANCH_DATA.phone
// menuItems = window.BRANCH_DATA.menu
// أوامر الفرع = window.BRANCH_DATA.orders
```

### حفظ طلب جديد:

```javascript
if (window.BRANCH_DATA) {
  window.BRANCH_DATA.orders.push(orderData);
  saveBranchData(window.BRANCH_DATA);
}
allOrders.push(orderData); // نسخة احتياطية
localStorage.setItem("allOrders", JSON.stringify(allOrders));
```

---

## 🚦 الحالة الحالية

✅ **مكتمل:**

- نظام المفاتيح الديناميكية
- استقلال بيانات الفروع
- لوحة تحكم منفصلة
- إعدادات الفرع
- رسائل الواتساب
- نظام التحميل الذكي
- إعادة التوجيه من البوابة

⚡ **جاهز للاختبار**

---

**تم بتاريخ:** 20 فبراير 2026  
**الإصدار:** 2.1 - نظام إدارة الفروع المستقل  
**حالة الترخيص:** Professional Grade ⭐⭐⭐⭐⭐
