# دليل الاستخدام السريع - نظام الفروع المستقل

## Quick Start Guide - My Order Multi-Branch System

---

## 🎬 خطوات الاختبار السريعة

### 1️⃣ **بدء التطبيق**

```bash
# شغّل الخادم:
START_SERVER.bat         # Windows
# أو
START_SERVER.ps1         # PowerShell

# أو افتح مباشرة:
http://localhost:8080/gateway.html
```

### 2️⃣ **اختيار فرع من البوابة**

- انقر على أي بطاقة فرع في البوابة
- سيتم نقلك إلى: `index.html?res=<branch-name>`

### 3️⃣ **اختبار الاستقلالية**

#### أ) اختبر تغيير الأسعار:

```
🍕 فرع 1: غير سعر البيتزا ← 100 ج.م
🍔 فرع 2: شاهد سعر البرجر ← 120 ج.م (لم يتغير!)
```

#### ب) اختبر رقم الواتساب:

```
🍕 فرع 1: اذهب للإعدادات → ادخل: 201001234567
    عند الطلب: wa.me/201001234567 ✅

🍔 فرع 2: اذهب للإعدادات → ادخل: 201101234567
    عند الطلب: wa.me/201101234567 ✅
```

#### ج) اختبر سجل الطلبات:

```
🍕 فرع 1: اطلب 2 بيتزا
    → اذهب لـ "سجل طلباتي"
    → تشاهد فقط طلبات البيتزا ✅

🍔 فرع 2: اطلب برجر
    → اذهب لـ "سجل طلباتي"
    → تشاهد فقط طلبات البرجر ✅
```

---

## 💾 ملفات التخزين (localStorage)

### التحقق من البيانات:

اضغط على F12 (أدوات المطور) → Console:

```javascript
// اعرض جميع الفروع المحفوظة:
Object.keys(localStorage).filter(k => k.includes('store_data'))

// مثال الإخراج:
[
  "store_data_مطعم-البيتزا-الإيطالي",
  "store_data_برجر-بازوكا",
  ...
]

// اعرض بيانات فرع معين:
JSON.parse(localStorage.getItem("store_data_مطعم-البيتزا-الإيطالي"))

// النتيجة:
{
  slug: "مطعم-البيتزا-الإيطالي",
  name: "مطعم البيتزا الإيطالي",
  phone: "201001234567",
  menu: [...items],
  orders: [...orders],
  description: "...",
  ...
}
```

---

## 🔧 قائمة الاختبارات

| الاختبار             | الخطوات                 | النتيجة المتوقعة                |
| -------------------- | ----------------------- | ------------------------------- |
| **1: اختيار الفرع**  | gateway.html → اختر فرع | ✅ يفتح index.html?res=<branch> |
| **2: تنسيق القائمة** | افتح فرعين مختلفين      | ✅ أسعار مختلفة لكل فرع         |
| **3: تعديل الأسعار** | غير السعر في فرع 1      | ✅ تأثر فرع 1، فرع 2 لم يتغير   |
| **4: رقم الواتساب**  | أضف رقم في الإعدادات    | ✅ الطلب يُرسل للرقم الجديد     |
| **5: سجل الطلبات**   | اطلب من فرعين           | ✅ كل عميل يرى طلباته فقط       |
| **6: لوحة التحكم**   | ادخل الإدارة من فرع     | ✅ الإحصائيات من هذا الفرع      |
| **7: إعادة التوجيه** | اكتب index.html مباشرة  | ✅ يُعاد فوراً إلى gateway.html |

---

## 📱 سيناريو عملي كامل

### السيناريو:

أنت تدير **3 فروع** متزامنة

### الخطوات:

#### **الفرع الأول: مطعم البيتزا**

```
1. gateway.html → اختر "مطعم البيتزا الإيطالي"
2. أضف طلب: 2 بيتزا + 1 عصير
3. الإعدادات: اكتب رقم واتساب خاص بك
4. أضف في البيانات:
   - اسم الفرع: "فرع الزمالك"
   - العنوان: "شارع 26 يوليو"
   - ساعات: "10:00 - 23:00"
5. احفظ البيانات → localStorage يحفظ في store_data_مطعم-البيتزا-الإيطالي
```

#### **الفرع الثاني: برجر بازوكا**

```
1. gateway.html → اختر "برجر بازوكا"
2. غيّر سعر البرجر إلى 150 (لا يؤثر على البيتزا!)
3. أضف طلب: 1 برجر
4. الإعدادات: رقم واتساب مختلف
5. احفظ → store_data_برجر-بازوكا
```

#### **الفرع الثالث: كافيه السوشي**

```
1. gateway.html → اختر "مطعم السوشي الياباني"
2. أضف قهوة جديدة برسم 50
3. أضف طلب
4. احفظ → store_data_مطعم-السوشي-الياباني
```

#### **التحقق النهائي:**

```javascript
// في Console:

// 1. اعرض الفروع المحفوظة:
Object.keys(localStorage).filter((k) => k.startsWith("store_data")).length;
// النتيجة: 3 أو أكثر ✅

// 2. اعرض طلبات فرع من الفروع:
const branch1 = JSON.parse(
  localStorage.getItem("store_data_مطعم-البيتزا-الإيطالي"),
);
branch1.orders.length; // عدد الطلبات
// النتيجة: 1 ✅

// 3. تأكد من استقلالية الأسعار:
const branch1Menu = branch1.menu;
const branch2Menu = JSON.parse(
  localStorage.getItem("store_data_برجر-بازوكا"),
).menu;
branch1Menu[0].price !== branch2Menu[0].price; // أسعار مختلفة
// النتيجة: true ✅
```

---

## ⚠️ نقاط مهمة

### ✅ يجب أن تحدث:

- كل فرع لديه **مفتاح تخزين وحيد**: `store_data_<slug>`
- **الأسعار** مختلفة لكل فرع
- **الطلبات** محفوظة في الفرع فقط
- **الواتساب** يستخدم رقم الفرع الخاص
- **البوابة** هي نقطة الدخول الوحيدة

### ❌ يجب ألا تحدث:

- تأثر فرع بتعديلات فرع آخر
- عرض طلبات فرع في فرع آخر
- استخدام رقم واتساب خاطئ
- فتح index.html مباشرة (بدون gateway)

---

## 🛠️ استكشاف الأخطاء

### المشكلة: "أسعار الفرع الثاني نفس الفرع الأول"

```javascript
// التحقق:
const branch1 = JSON.parse(
  localStorage.getItem("store_data_مطعم-البيتزا-الإيطالي"),
);
const branch2 = JSON.parse(localStorage.getItem("store_data_برجر-بازوكا"));

if (branch1.menu[0].price === branch2.menu[0].price) {
  console.log("❌ الأسعار متطابقة - خطأ التحديث");
} else {
  console.log("✅ الأسعار مختلفة بشكل صحيح");
}
```

### المشكلة: "سجل الطلبات يعرض جميع الطلبات"

```javascript
// التحقق:
const branch = JSON.parse(localStorage.getItem("store_data_برجر-بازوكا"));
console.log("عدد طلبات هذا الفرع:", branch.orders.length);
// يجب أن يكون ≤ عدد الطلبات الكلي ✅
```

### المشكلة: "الواتساب يعطي رقم خاطئ"

```javascript
// التحقق:
const branch = window.BRANCH_DATA;
console.log("رقم الفرع:", branch.phone);
console.log("رقم الواتساب المستخدم:", RESTAURANT_PHONE);
// يجب أن تكون متطابقة ✅
```

---

## 📊 ملخص الهيكل النهائي

```
gateway.html (البوابة الموحدة)
    ↓
index.html?res=<branch-name>
    ├─ CURRENT_RESTAURANT = branch-name
    ├─ BRANCH_DATA = loadBranchData()
    │   └─ store_data_<branch-name>
    │       ├─ name, phone, logo, hours
    │       ├─ menu (قائمة منفصلة)
    │       ├─ orders (طلبات الفرع)
    │       └─ description, addresses, social
    │
    ├─ Admin Panel
    │   ├─ تعديل قائمة الفرع
    │   ├─ عرض طلبات الفرع
    │   ├─ إعدادات الفرع
    │   └─ إحصائيات الفرع
    │
    └─ Customer View
        └─ قائمة الفرع + طلب + سجل الطلبات
```

---

## 🎓 ملاحظات تقنية

### للمطورين:

- استخدم `window.BRANCH_DATA` للوصول لبيانات الفرع الحالي
- استخدم `RESTAURANT_PHONE` للواتساب (يُحدَّث تلقائياً)
- احفظ البيانات باستخدام `saveBranchData()`

### للصيانة:

- لا تحذف مفاتيح `store_data_*` يدوياً
- استخدم console لتصحيح البيانات
- احتفظ بنسخة احتياطية من localStorage

---

## ✨ الخلاصة

**النظام الجديد يضمن:**

- ✅ عزل كامل بين الفروع
- ✅ إدارة سهلة ومستقلة
- ✅ لا تعارضات في البيانات
- ✅ قابلية تدرج عالية
- ✅ أمان وموثوقية

**جاهز للإنتاج! 🚀**

---

**آخر تحديث:** 20 فبراير 2026 | **الإصدار:** 2.1
